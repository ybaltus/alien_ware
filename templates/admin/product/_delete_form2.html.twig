<form method="post" action="{{ path('product_delete2', {'id': product.id}) }}" onsubmit="return confirm('{{'product.submitDelete'|trans}}');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
	<button class="btn btn-danger " title="{{'product.delete'|trans}}">
		{{'product.delete'|trans}}
	</button>
</form>
